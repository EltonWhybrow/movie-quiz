import { render, screen } from '@testing-library/react';
import MovieQuiz from './MovieQuiz';

// test('renders learn react link', () => {
//   render(<MovieQuiz />);
//   const linkElement = screen.getByText(/Movie Quiz/i);
//   expect(linkElement).toBeInTheDocument();
// });
